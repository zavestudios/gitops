apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mia-provider
  namespace: mia
  labels:
    zave.io/workload: mia
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-kv
  target:
    name: mia-provider
    creationPolicy: Owner
    template:
      type: Opaque
  data:
    - secretKey: ANTHROPIC_API_KEY
      remoteRef:
        key: tenants/mia/provider
        property: ANTHROPIC_API_KEY
